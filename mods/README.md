# Mods

Each mod lives in its own folder and starts with `mod.toml`. IDs use lowercase dotted namespaces; content keys use `namespace:local_name`. Dependency order is deterministic and validated before content loads.
